[pytest]
# Only our own tests. `secrets/` holds vendored CLI state (the Codex plugin
# cache ships its own failing test suite) and must never be collected.
testpaths = tests
norecursedirs = secrets .git .venv node_modules
