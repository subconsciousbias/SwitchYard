# ---- gateway ---------------------------------------------------------------
LITELLM_MASTER_KEY=sk-switchyard-change-me   # what your tools authenticate with
GATEWAY_PORT=4000
PORTAL_PORT=4001
GATEWAY_WORKERS=2
POSTGRES_PASSWORD=change-me

# ---- workhorse plans -------------------------------------------------------
MINIMAX_ULTRA_API_BASE=https://api.minimax.io/v1
MINIMAX_ULTRA_API_KEY=
MINIMAX_MAX_API_BASE=https://api.minimax.io/v1
MINIMAX_MAX_API_KEY=
GLM_API_BASE=https://open.bigmodel.cn/api/paas/v4
GLM_API_KEY=
OPENROUTER_API_KEY=

# ---- judgement plans -------------------------------------------------------
ANTHROPIC_API_KEY=
XAI_API_KEY=
ASTRA_API_BASE=
ASTRA_API_KEY=

# ---- OpenAI $100 seat (OAuth, via sidecar — NOT an API key) -----------------
# A seat has no API key. The Codex CLI owns the login; point this at a host
# ~/.codex that is already logged in, or run `codex login` in the container.
CODEX_CONFIG_DIR=/Users/temporalis/.codex
CODEX_MODEL=gpt-5
# Flags differ between Codex releases; check `codex exec --help` if calls fail.
CODEX_EXTRA_ARGS=

# ---- Claude Max (OAuth, via sidecar) ---------------------------------------
# Point at a host ~/.claude that is already logged in, or log in inside the
# container once. No API key involved.
CLAUDE_CONFIG_DIR=/Users/temporalis/.claude
CLAUDE_MODEL=opus
# Extra model aliases the Claude sidecar may run, comma-separated. Find the
# valid names with:  claude --model bogus 2>&1 | head
# Leave empty until confirmed; an unlisted alias falls back to CLAUDE_MODEL.
CLAUDE_MODEL_ALLOW=

# ---- local models ----------------------------------------------------------
# Ollama: http://host.docker.internal:11434/v1 · LM Studio: ...:1234/v1
LOCAL_API_BASE=http://host.docker.internal:11434/v1

# ---- OpenCode Go (cancelled, expires 2026-09-27) ---------------------------
OPENCODE_API_BASE=
OPENCODE_API_KEY=
