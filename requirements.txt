# Used by the portal image (python:3.12-slim) and the sidecar image.
#
# NOT used by the gateway: that image is venv-based with no pip, and already
# ships pyyaml, redis, fastapi, httpx and jinja2 because LiteLLM depends on them.
# Dockerfile.gateway asserts their presence instead of installing anything.
pyyaml>=6.0
redis>=5.0
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
httpx>=0.27
python-multipart>=0.0.9
